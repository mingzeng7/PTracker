// An example configuration file that stores information about a store.

//output_file = "output.h5";

//if_RR = false;
if_RR1 = false;

sampling_factor = 32.0;
t1 = 1.e8;

particle =
{
  allocation_method = 0; // 0 means allocate particles one by one. Single particle mode.
  single_particle = ({A = 2.0;
                      gamma0 = 1e5;
                      zeta0 = 0.;
                      phase0 = 0.;}
                    );
};

wake =
{
  gamma_w = 5614.542887235088;
}

random =
{
// time_seed = true;
//  seed = 3;
}

